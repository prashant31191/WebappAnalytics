$(function(){
	
	/*
	$('#activePhoto').load(function(){
		$(this).fadeIn();
		$('#activePhoto-wrapper').height($('this').height());
	});
	
	//init
	if(!location.hash.length){
		var virtualHash = $('.photo-thumbnail-primary').eq(0).attr('photoId').replace(/\#/,'');
		$('#activePhoto').attr('src',$('a[photoId='+virtualHash+']').eq(0).attr('href'));
	}
	
	
	
	$('.photo-thumbnail').bind('click',function(e){
		
		$('#activePhoto').fadeOut();
		
		
		$('#activePhoto').attr('src',$(this).attr('href'));
		
		
		return false;
	});
	*/
	
	
});
